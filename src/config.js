// configurable information for the rest of the js code
const config = {
  ids: {
    entries: 'cards',
    addButton: 'add-button',
  },
  locations: {
    addPage: '/add.html',
  },
};

export default config;
